# Detail Readability Review

- Default detail headings use `detailHeading`, labels use `detailLabel`, body uses `detailBody`, and source paths use `detailSource`.
- Warning/error/status diagnostics retain dedicated diagnostic roles.
- Maintainer smoke review: PASS. The default palette separates headings, labels, source metadata, and body text without relying on table borders.
