# Documentation Inventory

## Existing Hotkey Documentation

- `README.md` documents the global hotkey config path, JSON shape, run options,
  and `R` reload control.
- `specs/002-configurable-dashboard-ui/quickstart.md` already describes the
  target UI preference examples and verification commands for this feature.

## Required Updates

- Update `README.md` so the hotkey section becomes a combined dashboard
  preferences section covering `bindings` and `ui`.
- Add the supported color roles, named and hex color formats, layout modes, the
  automatic 120-column threshold, reload behavior, and invalid-preference
  fallback behavior.
- Refresh `specs/002-configurable-dashboard-ui/quickstart.md` after
  implementation with the final commands and examples.
